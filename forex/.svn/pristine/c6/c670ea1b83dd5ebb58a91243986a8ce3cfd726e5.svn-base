package com.talent.forex.init;

import javax.servlet.ServletContext;

import com.talent.forex.constant.ApplicationNameConst;
import com.talent.forex.util.SwapUtil;

public class SwapTableInit implements Init {

	@Override
	public void init(ServletContext context) {
		context.setAttribute(ApplicationNameConst.SWAP_UTIL, SwapUtil.getInstance());

	}

}
